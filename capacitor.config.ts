import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.canario.inventario',
  appName: 'Inventario',
  webDir: 'dist/inventario/browser'
};

export default config;

